---
description: Verify if the code is correct
---

1. Static Analysis (The Guardrails)Command: ./gradlew detektPurpose: Ensures Kotlin code adheres to the project's complexity and style rules.Action: If Detekt fails, refactor the specific files flagged. Do not ignore "Magic Number" or "Complex Method" warnings.2. Type & Compilation SafetyCommand: ./gradlew compileKotlinMetadataPurpose: Validates that the Common code is actually platform-agnostic and doesn't accidentally use platform-specific APIs (like java.io or Foundation).Action: If compilation fails, move platform-specific logic to expect/actual blocks.3. Behavioral Validation (Test Runners)Command: ./gradlew allTestsPurpose: Executes logic across all configured targets (Desktop, iOS, etc.).Action: If a test passes on JVM but fails on iOS (e.g., due to Kotlin/Native memory management or Long bit-width differences), you must analyze the platform-specific implementation in src/iosMain.4. LLM-as-Judge (Logic Review)Process: Before finishing, perform a self-scan of the logic against docs/SYNAPSE_CORE.md.Checklist: * Does this note-linking logic maintain $O(n + m)$ complexity as per KMP requirements?Are there any accidental references to android.* libraries in a Desktop-only project?Self-Repair LoopIf any sensor above returns an error, follow this loop:Read the specific stack trace or linter violation.Locate the file and line number.Correct the code.Re-run the specific sensor command (e.g., ./gradlew desktopTest) to verify the fix.Repeat until all sensors return a "SUCCESS" status.Note: Only surface errors to the user if the sensor failure indicates an architectural conflict that requires human decision-making. Otherwise, repair it silently.